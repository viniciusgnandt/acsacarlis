# Override para buildar a imagem localmente em vez de baixar do registry.
# Uso: docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: acsacarlis-site:local
