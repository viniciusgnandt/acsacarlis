{
  "name": "acsa-carlis-site",
  "private": true,
  "version": "1.0.0",
  "description": "Build assets for acsacarlis.adv.br",
  "scripts": {
    "build": "node scripts/build-html.js && tailwindcss -i ./src/tailwind-input.css -o ./site/assets/tailwind.css --minify",
    "dev": "npm run build && concurrently -n html,css -c blue,magenta \"nodemon --watch src/pages --watch partials --ext html --exec \\\"node scripts/build-html.js\\\"\" \"tailwindcss -i ./src/tailwind-input.css -o ./site/assets/tailwind.css --watch\""
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "nodemon": "^3.1.14",
    "tailwindcss": "^3.4.13"
  }
}
